name: Events Demo 1
on: 
  pull_request:
    types:
      - opened
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub context
        run: echo "${{ toJSON(github) }}"
      - name: Output GitHub context
        run: echo "${{ github.sha }}"
      - name: Install dependencies
        run: npm ci